//todo Import components
import { Main } from "../../../components";

export const ForgotPassword = () => {
  return <Main className="h-screen w-full flex items-center justify-center text-4xl">ForgotPassword</Main>;
};
